<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <div class="ibox-title">
                <h4 class="card-title">Deneyim</h4>
                <input style="float:right" type="button" onClick="location.href='/admin/Deneyim/Insert'" class="btn btn-primary" value="Yeni Ekle"> <br /><br /><br />
            </div>

            <div class="ibox-content">
                <div class="table-responsive">
                    <table class="table table-striped table-bordered table-hover">
                        <thead>
                            <tr>
                                <th>Sıra No</th>
                                <th>Kişi</th>
                                <th>Deneyim Adı</th>
                                <th>Açıklama</th>
                                <th>Başlangıç Tarihi</th>
                                <th>Bitiş Tarihi</th>
                                <th>İşlemler</th>

                            </tr>
                        </thead>
                        <tbody>
                            @{
                                int i = 1;
                                foreach (var item in Model)
                                {
                                    <tr>
                                        <td>@Html.Raw(@i++)</td>

                                        @foreach (var item1 in ViewBag.Kisiler)
                                        {
                                            @if (item1.UID == item.PersonalInformationUID)
                                            {
                                                <td>
                                                    @item1.NameSurname
                                                </td>
                                            }
                                        }
                                        <td>@item.Name</td>
                                        <td>@item.Description</td>
                                        <td>@item.StartedDate</td>
                                        <td>@item.EndDate</td>
                                        <td>
                                            <a href="/Admin/Deneyim/Update/@item.UID" class="badge badge-gradient-success">Güncelle</a>
                                            <a href="/Admin/Deneyim/Delete/@item.UID" class="badge badge-gradient-danger">Sil</a>
                                        </td>
                                    </tr>
                                }
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

        @if (TempData["Result"] != null)
        {
            if (TempData["Result"].ToString() == Core.Results.ComplexTypes.StatusCode.Success.ToString())
            {
                <div class="alert alert-success">İşlem Başarılı</div>
            }
            else
            {
                <div class="alert alert-danger">İşlem Başarısız</div>
            }
        }
    </div>
</div>